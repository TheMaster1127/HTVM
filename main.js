

function MsgBox(value) {
	console.log(value)
}



var qWASED;
let arr123 = [];
arr123.push(6);
arr123.push(5);
arr123.push(58);
// You can use indentations if you want to. It's all optional.
for (let A_Index1 = 1; A_Index1 <= arr123.length + 1 - 1; A_Index1++) {
    MsgBox(arr123[A_Index1 - 1]);
}
// You can use indentations if you want to. It's all optional.
for (let A_Index2 = 1; A_Index2 <= 3 + 1 - 1; A_Index2++) {
    MsgBox(A_Index2);
}
while (AWSEDS && WER) {
    aQwsde
}
let result = (age >= 18) ? "Adult" : "Minor";
// You can use indentations if you want to. It's all optional.
if (arr123[1] == 5) {
    MsgBox("yes");
}
else if (asd) {
    AwSZDF
} else {
    aszd
}